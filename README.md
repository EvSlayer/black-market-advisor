# Black Market Advisor

Modularized static site.

- `index.html` — page structure
- `styles.css` — styling
- `js/app.js` — application logic

Open `index.html` with VS Code Live Server for local testing.
